/// @description Insert description here
// You can write your code in this editor
if (originalY - y < hoverHeight) {
	for(ii = 0; ii < popUpIncr; ii++) {
		if (--y == originalY - hoverHeight) {
			break;
		}
	}
}
if (i++ > ttl) {
	instance_destroy(self);
}
